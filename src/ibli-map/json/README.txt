This directory is for placing the JSON of the divisions.

The JSON file must be named kenya.json in order for the map to work.

The file must be formatted according to geoJson standards,
and each feature's properties must include the property DIVISION which is the division name, and the property DIV_ID which is the division ID.
